services:
  - type: web
    name: claws-server
    runtime: docker
    dockerfilePath: server/Dockerfile
    dockerContext: server
    plan: free
    envVars:
      - key: PORT
        value: 2567
    healthCheckPath: /
